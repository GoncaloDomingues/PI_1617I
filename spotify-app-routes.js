"use strict"
const dependencies = require("./dependencies.js");
const spotify = require("./spotify")

exports.routes=function routes(server){

    server.get("/", function homepage(req,res,next){

    })
}